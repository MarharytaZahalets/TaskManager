import { StyleSheet } from 'react-native';

import { Colors } from 'core/theme/colors';

const styles = StyleSheet.create({
  header: { color: Colors.text, fontSize: 20 },
});

export default styles;
