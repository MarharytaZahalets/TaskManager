import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: { bottom: 30, position: 'absolute', right: 0 },
  header: { paddingBottom: 40 },
  welcomeText: { paddingBottom: 24 },
});

export default styles;
