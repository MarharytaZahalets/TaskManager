import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { marginHorizontal: 0, padding: 0 },
  coordinatesContainer: { padding: 20 },
  mapView: { flex: 1 },
  text: { fontSize: 16, lineHeight: 24 },
});

export default styles;
